version: '3'
services:
  react-webapp:
    build: ./react-webapp
    volumes:
      - ./react-webapp:/usr/src/app
    ports:
      - "3000:3000"
    depends_on:
      - python-api-server

  python-api-server:
    build: ./python-api-server
    volumes:
      - ./python-api-server:/usr/src/app
    ports:
      - "5000:5000"